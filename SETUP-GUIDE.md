# FORA – Οδηγός Εγκατάστασης (για μη τεχνικό χρήστη)

Αυτός ο οδηγός σας καθοδηγεί βήμα-βήμα, χωρίς να χρειάζεται να γράψετε ή να καταλάβετε κώδικα. Θα κάνετε αντιγραφή-επικόλληση μερικές εντολές. Αφιερώστε 30–45 λεπτά την πρώτη φορά.

Ο νέος ιστότοπος αποτελείται από **δύο κομμάτια**:

1. **Strapi** — ο «πίνακας διαχείρισης». Ανοίγει σε ένα παράθυρο του browser και έχει φόρμες όπου γράφετε και ανεβάζετε εικόνες. Εδώ αλλάζετε το περιεχόμενο.
2. **Next.js** — ο δημόσιος ιστότοπος που βλέπουν οι επισκέπτες. Διαβάζει αυτόματα ό,τι έχετε στο Strapi και το δείχνει, πολύ γρήγορα.

Και τα δύο βρίσκονται μέσα στον φάκελο `fora-website` στην επιφάνεια εργασίας σας:

```
Fora/
└── fora-website/
    ├── backend/    ← ο πίνακας διαχείρισης (Strapi)
    └── frontend/   ← ο δημόσιος ιστότοπος (Next.js)
```

---

## Τι θα χρειαστείτε (μία φορά)

### 1. Node.js
Είναι το «περιβάλλον» που τρέχει και τα δύο κομμάτια.

1. Πηγαίνετε στο **https://nodejs.org**
2. Κατεβάστε την έκδοση που λέει **«LTS»** (συνιστάται η **20** ή **22**).
3. Ανοίξτε το αρχείο που κατέβηκε και πατήστε συνέχεια/Next σε όλα, μέχρι να τελειώσει.

> ⚠️ Το Strapi δουλεύει με Node.js εκδόσεις **18 έως 22**. Μην εγκαταστήσετε την έκδοση 23 ή νεότερη.

### 2. Το «Τερματικό» (Terminal)
Είναι ένα παράθυρο όπου γράφετε εντολές. Δεν είναι επικίνδυνο.

- **Windows:** πατήστε το πλήκτρο Windows, γράψτε `PowerShell`, και ανοίξτε το **Windows PowerShell**.
- **Mac:** πατήστε `Cmd + Space`, γράψτε `Terminal`, Enter.

Μια χρήσιμη εντολή: για να «μπείτε» σε έναν φάκελο γράφετε `cd` και μετά τη διαδρομή του. Θα σας δίνω κάθε φορά ακριβώς τι να γράψετε.

### 3. Τι χρειάζεται το Strapi για να «ζήσει» (για αργότερα, στον διακομιστή σας)
Επειδή θα κάνετε μόνοι σας τη φιλοξενία (hosting), να ξέρετε ότι το Strapi χρειάζεται:

- **Node.js** (έκδοση 18–22) στον διακομιστή.
- **Μια βάση δεδομένων.** Για δοκιμές στον υπολογιστή σας χρησιμοποιεί ένα απλό αρχείο (SQLite) — δεν χρειάζεται να εγκαταστήσετε τίποτα. Για τον πραγματικό διακομιστή συνιστάται **PostgreSQL** (μια δωρεάν, δημοφιλής βάση δεδομένων).
- **Χώρο στον δίσκο** για τα αρχεία που ανεβάζετε (φωτογραφίες, PDF). Αποθηκεύονται στον φάκελο `backend/public/uploads`.

Δεν χρειάζεται να καταλάβετε τα παραπάνω τώρα — απλώς δώστε τα σε όποιον σας βοηθήσει με τον διακομιστή. Οι τοπικές δοκιμές παρακάτω δεν χρειάζονται τίποτα από αυτά.

---

## ΜΕΡΟΣ Α — Ξεκινήστε τον πίνακα διαχείρισης (Strapi)

### Βήμα 1 — Ανοίξτε το τερματικό μέσα στον φάκελο του Strapi
Αντιγράψτε και επικολλήστε (προσαρμόστε τη διαδρομή αν χρειάζεται). Σε **Windows PowerShell**:

```
cd "$env:USERPROFILE\Desktop\Fora\fora-website\strapi"
```

Σε **Mac**:

```
cd ~/Desktop/Fora/fora-website/strapi
```

### Βήμα 2 — Φτιάξτε αυτόματα τα «μυστικά κλειδιά»
Γράψτε:

```
node scripts/setup-env.js
```

Αυτό δημιουργεί ένα αρχείο ρυθμίσεων με ασφαλή τυχαία κλειδιά. **Δεν χρειάζεται να δείτε ή να καταλάβετε τα κλειδιά.**

### Βήμα 3 — Εγκαταστήστε τα απαραίτητα προγράμματα
Γράψτε:

```
npm install
```

Θα πάρει μερικά λεπτά και θα «τρέξουν» πολλές γραμμές. Περιμένετε να σταματήσει και να σας επιστρέψει τον κέρσορα.

### Βήμα 4 — Ξεκινήστε το Strapi για πρώτη φορά
Γράψτε:

```
npm run develop
```

Μόλις σταματήσουν τα μηνύματα, ανοίγει μόνο του (ή ανοίξτε εσείς) ο browser στη διεύθυνση **http://localhost:1337/admin**.

Θα δείτε μια φόρμα **«Δημιουργία διαχειριστή»**. Συμπληρώστε όνομα, email και έναν κωδικό (κρατήστε τον — είναι ο κωδικός σας για τον πίνακα). Πατήστε **«Let's start / Ξεκινάμε»**.

> 🎉 Μπήκατε στον πίνακα διαχείρισης. Προς το παρόν είναι άδειος — στο επόμενο βήμα θα γεμίσει αυτόματα με το πραγματικό περιεχόμενο.

### Βήμα 5 — Σταματήστε προσωρινά το Strapi
Πηγαίνετε στο τερματικό και πατήστε **`Ctrl + C`** (κρατήστε Ctrl και πατήστε C). Αυτό σταματά το Strapi για λίγο, ώστε να τρέξει η αυτόματη εισαγωγή.

### Βήμα 6 — Εισάγετε αυτόματα ΟΛΟ το περιεχόμενο (2022–2025)
Γράψτε:

```
npm run seed
```

Αυτό το πρόγραμμα:

- **Κατεβάζει** από τον παλιό ιστότοπο όλες τις φωτογραφίες, τα λογότυπα και τα PDF και τα **αποθηκεύει μέσα στο δικό σας Strapi** (ώστε ο νέος ιστότοπος να μην εξαρτάται πια από τον παλιό).
- **Δημιουργεί** τις 4 διοργανώσεις (Forum 2022, 2023, 2024, 2025) με τα πραγματικά κείμενα, τους ομιλητές, τις θεματικές, τους χορηγούς, τις φωτογραφίες και τα βίντεο.
- Συμπληρώνει τις ρυθμίσεις (λογότυπο, email, τηλέφωνο, Facebook, LinkedIn).

Θα δείτε μηνύματα προόδου (π.χ. «κατέβηκαν 45 αρχεία»). Επειδή κατεβαίνουν εκατοντάδες φωτογραφίες, **μπορεί να πάρει 5–15 λεπτά.** Περιμένετε να δείτε «Ολοκληρώθηκε».

> Αν κάποιες γραμμές λένε «Δεν κατέβηκε…», σημαίνει ότι λίγες μεμονωμένες εικόνες δεν βρέθηκαν στον παλιό ιστότοπο. Δεν είναι πρόβλημα — τα υπόλοιπα μπαίνουν κανονικά και μπορείτε να προσθέσετε ό,τι λείπει από τον πίνακα αργότερα.

### Βήμα 7 — Ξεκινήστε ξανά το Strapi
```
npm run develop
```

Ανοίξτε πάλι **http://localhost:1337/admin**. Τώρα, στα αριστερά, στο **«Content Manager»**, θα δείτε **Forum (Διοργάνωση)** με 4 εγγραφές και τις **Ρυθμίσεις Ιστότοπου** συμπληρωμένες.

**Το Strapi είναι έτοιμο.** Αφήστε αυτό το τερματικό ανοιχτό (το Strapi πρέπει να «τρέχει» όσο δουλεύετε τον ιστότοπο).

---

## ΜΕΡΟΣ Β — Ξεκινήστε τον δημόσιο ιστότοπο (Next.js)

### Βήμα 8 — Ανοίξτε ΕΝΑ ΝΕΟ τερματικό στον φάκελο του Next.js
(Μην κλείσετε το προηγούμενο.) Ανοίξτε ένα δεύτερο παράθυρο PowerShell/Terminal και γράψτε:

**Windows:**
```
cd "$env:USERPROFILE\Desktop\Fora\fora-website\frontend"
```
**Mac:**
```
cd ~/Desktop/Fora/fora-website/frontend
```

### Βήμα 9 — Φτιάξτε το αρχείο ρυθμίσεων του ιστότοπου
**Windows:**
```
copy .env.example .env.local
```
**Mac:**
```
cp .env.example .env.local
```
Για τις τοπικές δοκιμές οι προεπιλογές είναι εντάξει — δεν χρειάζεται να αλλάξετε τίποτα.

### Βήμα 10 — Εγκαταστήστε και ξεκινήστε τον ιστότοπο
```
npm install
```
και μετά:
```
npm run dev
```
Ανοίξτε **http://localhost:4321** στον browser. **Θα δείτε τον νέο ιστότοπο** με όλο το περιεχόμενο! 🎉

### Βήμα 11 — Έλεγχος της «τελικής» έκδοσης (προαιρετικό, τοπικά)
```
npm run build
npm run start
```
Ο ιστότοπος τρέχει σαν διακομιστής (δεν είναι πια στατικά αρχεία): διαβάζει το
περιεχόμενο από το Strapi **την ώρα της επίσκεψης**. Άρα ό,τι αλλάζετε στο Strapi
φαίνεται με ένα refresh, χωρίς νέο build.

> Για το ανέβασμα στον διακομιστή (Docker + Portainer) δείτε το **DEPLOY.md**.

---

## ⭐ Η ΕΤΗΣΙΑ ΕΝΗΜΕΡΩΣΗ — πώς προσθέτετε νέο Forum (χωρίς κώδικα)

Αυτό είναι το πιο σημαντικό. Κάθε χρόνο:

1. Ανοίξτε **http://localhost:1337/admin** (ή τη διεύθυνση του Strapi στον διακομιστή σας) και συνδεθείτε.
2. Αριστερά: **Content Manager → Forum (Διοργάνωση) → Create new entry** (Δημιουργία νέας εγγραφής).
3. Συμπληρώστε τις φόρμες (όλα τα ονόματα πεδίων είναι στα Ελληνικά):
   - **Αριθμός** (π.χ. 5) και **Έτος** (π.χ. 2026)
   - **Θέμα** (ο τίτλος της χρονιάς) και προαιρετικά **Υπότιτλος**
   - **Slug**: πατήστε το κουμπάκι δίπλα για να δημιουργηθεί αυτόματα (π.χ. `forum-2026`)
   - **Ημερομηνία** και **Χώρος**
   - **Εμφάνιση (στυλ):** κάθε Forum μπορεί να έχει δικό του χρώμα και ύφος:
     - **Θέμα Εμφάνισης** — διαλέξτε ένα έτοιμο χρώμα από τη λίστα: Κλασικό (μπλε), Πετρόλ, Πράσινο, Μωβ, Μπορντό, Σκούρο, Πορτοκαλί. Αλλάζουν αυτόματα τα κουμπιά, οι τόνοι, η κεφαλίδα (hero) και τα φόντα των ενοτήτων.
     - **Γραμματοσειρά Τίτλων** — προαιρετικά αλλάξτε τη γραμματοσειρά των τίτλων (Hagrid, Poppins, Montserrat, Playfair, Oswald).
     - **Κύριο Χρώμα (override)** και **Χρώμα Τόνου (override)** — προαιρετικά· αφήστε τα κενά για να ισχύσει το «Θέμα Εμφάνισης». Αν θέλετε ακριβές δικό σας χρώμα, πατήστε το τετραγωνάκι χρώματος και διαλέξτε με το ποντίκι (color picker) — δεν χρειάζεται να ξέρετε κωδικούς.
   - **Σύνδεσμος Εγγραφής** (η διεύθυνση της φόρμας εγγραφής)
   - **Ατζέντα (PDF)**: σύρετε το αρχείο PDF μέσα
   - **Θεματικές Ενότητες**: πατήστε «Add an entry» για κάθε ενότητα (τίτλος + περιγραφή)
   - **Ομιλητές**: πατήστε «Add an entry» για κάθε ομιλητή — φωτογραφία, όνομα, ιδιότητα, βιογραφικό. Για να αφαιρέσετε κάποιον, πατήστε τον κάδο.
   - **Συνεργάτες**: για κάθε κατηγορία (π.χ. «Χρυσός Χορηγός») προσθέτετε τα λογότυπα.
   - **Φωτογραφίες**: σύρετε μέσα όσες θέλετε.
   - **Βίντεο**: επικολλήστε συνδέσμους YouTube.
4. **ΠΟΛΥ ΣΗΜΑΝΤΙΚΟ — ο διακόπτης «Τρέχον Forum»:**
   - Στη **νέα** εγγραφή ανοίξτε τον διακόπτη **«Τρέχον Forum» → ON**. Αυτό την κάνει την αρχική σελίδα.
   - Στην **περσινή** εγγραφή γυρίστε τον **«Τρέχον Forum» → OFF**. Έτσι πηγαίνει αυτόματα στο αρχείο «Προηγούμενα Fora».
5. Πατήστε **Save** και μετά **Publish** (πάνω δεξιά).
6. Τελειώσατε! Ο ιστότοπος διαβάζει το Strapi ζωντανά — κάντε **refresh** τη σελίδα και το νέο Forum εμφανίζεται. (Δεν χρειάζεται νέο build ούτε ανέβασμα αρχείων.)

Αυτό ήταν. Καμία επαφή με κώδικα.

### Άλλες συνηθισμένες αλλαγές
- **Αλλαγή ημερομηνίας/θέματος/χώρου:** Content Manager → Forum → ανοίξτε την εγγραφή → αλλάξτε το πεδίο → Save → Publish.
- **Προσθήκη/αφαίρεση ομιλητή:** στην εγγραφή, ενότητα «Ομιλητές» → «Add an entry» ή κάδος → Save → Publish.
- **Αλλαγή λογότυπου ή στοιχείων επικοινωνίας:** Content Manager → **Ρυθμίσεις Ιστότοπου** → αλλάξτε → Save.
- **Αντικατάσταση της Ατζέντας (PDF):** στην εγγραφή, πεδίο «Ατζέντα (PDF)» → αφαιρέστε το παλιό, σύρετε το νέο → Save → Publish.
- **Αλλαγή στυλ/χρωμάτων μιας διοργάνωσης:** Content Manager → Forum → ανοίξτε την εγγραφή → αλλάξτε «Θέμα Εμφάνισης» ή/και «Γραμματοσειρά Τίτλων» → Save → Publish. Κάθε Forum εμφανίζεται με το δικό του χρώμα και στη σελίδα του και στην αρχική (αν είναι το τρέχον).

---

## Δύο πράγματα που αξίζει να ξέρετε

### 1. Τα ονόματα των χορηγών του 2025
Είπατε ότι θα δώσετε αργότερα τα ονόματα του **Χρυσού Χορηγού** και των **Υποστηρικτών** του 2025. Τα λογότυπα εμφανίζονται ήδη. Όταν έχετε τα ονόματα: Content Manager → Forum 2025 → «Συνεργάτες» → στο κάθε λογότυπο συμπληρώστε το πεδίο **«Όνομα»** → Save → Publish.

### 2. Οι γραμματοσειρές (Adobe Fonts)
Ζητήσατε να κρατήσουμε τις ακριβείς γραμματοσειρές του παλιού ιστότοπου (**Hagrid** και **Cambo**). Αυτές είναι πληρωμένες γραμματοσειρές της **Adobe Fonts** και είναι δεμένες με τον λογαριασμό/διεύθυνση του παλιού ιστότοπου.

- Στις **τοπικές δοκιμές** μπορεί να φαίνονται σωστά ή όχι, ανάλογα με τον λογαριασμό.
- Για τον **πραγματικό σας ιστότοπο** πρέπει να φτιάξετε ένα δικό σας δωρεάν «Web Project» στο **https://fonts.adobe.com** (χρειάζεται συνδρομή Adobe), να προσθέσετε τις Hagrid και Cambo, να πάρετε τον δικό σας σύνδεσμο (μοιάζει με `https://use.typekit.net/XXXXXXX.css`) και να τον βάλετε στο αρχείο `frontend/.env.local` στη γραμμή `NEXT_PUBLIC_TYPEKIT_URL=...`.
- Αν κάποια στιγμή δεν θέλετε την Adobe, πείτε μου και βάζω παρόμοιες δωρεάν γραμματοσειρές — η εμφάνιση θα μείνει σχεδόν ίδια.

---

## Όταν το ανεβάσετε στον διακομιστή σας (σύντομα)

Δώστε αυτά σε όποιον κάνει τη φιλοξενία:

1. Το **Strapi** τρέχει σαν εφαρμογή Node.js (18–22), με βάση **PostgreSQL**, και χρειάζεται μόνιμο χώρο για τον φάκελο `public/uploads`. Οι ρυθμίσεις μπαίνουν στο αρχείο `backend/.env` (βάλτε `DATABASE_CLIENT=postgres` και τα στοιχεία της βάσης).
2. Στο `backend/.env`, στη γραμμή `CORS_ORIGINS`, βάλτε τη δημόσια διεύθυνση του ιστότοπου (π.χ. `https://www.healthcare-management.gr`).
3. Στο `frontend/.env.local`, βάλτε `NEXT_PUBLIC_STRAPI_URL=` τη διεύθυνση του Strapi στον διακομιστή (π.χ. `https://admin.healthcare-management.gr`) και `NEXT_PUBLIC_SITE_URL=` τη δημόσια διεύθυνση.
4. Ο δημόσιος ιστότοπος (`frontend`) τρέχει σαν διακομιστής (Next.js standalone) μέσα σε Docker. Διαβάζει το Strapi ζωντανά, οπότε **δεν** χρειάζεται νέο build όταν αλλάζετε περιεχόμενο. Πλήρεις οδηγίες: **DEPLOY.md**.

---

## Αν κάτι πάει στραβά

- **«command not found» ή «npm δεν αναγνωρίζεται»:** δεν εγκαταστάθηκε σωστά το Node.js. Επαναλάβετε την εγκατάσταση από το nodejs.org και ανοίξτε νέο τερματικό.
- **Ο ιστότοπος (4321) δείχνει άδειος ή σφάλμα σύνδεσης:** βεβαιωθείτε ότι το Strapi «τρέχει» (το τερματικό του Μέρους Α είναι ανοιχτό στο `npm run develop`).
- **Το `npm run seed` σταμάτησε στη μέση:** τρέξτε το ξανά — όσα Forum μπήκαν ήδη τα προσπερνά και συνεχίζει.
- **Θέλετε να ξεκινήσετε από την αρχή το περιεχόμενο:** σβήστε το αρχείο `backend/.tmp/data.db` και τον φάκελο `backend/public/uploads`, μετά ξανά από το Βήμα 4.
- **Άλλαξα κάτι στο Strapi και δεν φαίνεται στον ιστότοπο:** κάντε **refresh** τη σελίδα. Ο ιστότοπος διαβάζει το Strapi ζωντανά (τοπικά και στον διακομιστή). Βεβαιωθείτε επίσης ότι πατήσατε **Publish** στο Strapi.
- **Δεν βλέπω τον επιλογέα χρώματος (color picker) — ή το Strapi δεν ξεκινά με μήνυμα «color-picker is not installed»:** λείπει το πρόσθετο. Στον φάκελο `strapi` τρέξτε ακριβώς αυτό (εγκαθιστά μόνο το πρόσθετο, είναι γρήγορο):

  ```
  npm install @strapi/plugin-color-picker@5.11.3
  ```

  και μετά ξεκινήστε ξανά:

  ```
  npm run develop
  ```

Για οτιδήποτε, στείλτε μου μήνυμα και σας καθοδηγώ.
